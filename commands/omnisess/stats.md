---
description: Show tool-call counts and file-I/O activity from the transcript SQLite index. Aggregates over a window (default 7d) or a single session.
argument-hint: "[--session <id>] [--window <duration>] [--tool <name>] [--full] [--json]"
allowed-tools: Bash
---

Invoke the omnisess:stats skill (using the Skill tool) and follow its instructions.
